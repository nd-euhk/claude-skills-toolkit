# Workflow Fixes — Todo List

**Ngày:** 2026-07-30
**Nguồn:** workflow-knowledge review
**Context:** Xem report đầy đủ tại `.work/reports/workflow-review-2026-07-30.md`

---

## Task 1: Sửa repoPath trong automation workflow

- **File:** `.claude/workflows/automation/workflow-sdlc-automation.js`
- **Dòng:** 28
- **Hiện tại:** `repoPath = process.env.HOME + '/projects/AI/Kit/toolkit'`
- **Target:** Bỏ `process.env.HOME`, pass `repoPath` từ args với default `'.'`
- **Ưu tiên:** HIGH
- **Trạng thái:** ✅ done

## Task 2: Thêm schema cho phase agent calls trong automation

- **File:** `.claude/workflows/automation/workflow-sdlc-automation.js`
- **Thay đổi:**
  - `PHASE_RESULT.required` → `[]` (bỏ required field để agent không fail validation)
  - Thêm `schema: PHASE_RESULT` vào `agent()` call trong `runPhase()` (dòng 475)
  - Logic extract mới: object result → dùng `result.outputs`/`result.frIds` trực tiếp; string result → regex fallback
  - Cập nhật 6 prompt functions (srsPrompt, hldPrompt, lldPrompt, impPrompt, tstPrompt, crossCuttingPrompt) với `## Output Format (REQUIRED — return as JSON)` section
  - Giữ `extractOutputs()` / `extractFrIds()` làm fallback
- **Ưu tiên:** HIGH
- **Trạng thái:** ✅ done

## Task 2: Thêm schema cho phase agent calls trong automation

- **File:** `.claude/workflows/automation/workflow-sdlc-automation.js`
- **Thay đổi:**
  - Thêm `schema` parameter vào `runPhase()` function
  - Default `PHASE_RESULT` schema cho các phase agents (sdlc-srs, sdlc-hld, sdlc-lld, sdlc-imp, sdlc-tst)
  - Dùng structured output thay vì `extractOutputs()` / `extractFrIds()` regex parsing
  - Fallback regex nếu schema không available
- **Ưu tiên:** HIGH
- **Trạng thái:** ✅ done

## Task 3: Parallelize verifySRSForDomains trong codebase-reverse

- **File:** `.claude/workflows/codebase/workflow-codebase-reverse.js`
- **Dòng:** 278-335
- **Hiện tại:** Sequential `for` loop
- **Target:** `parallel(domains.map(d => () => agent(...)))` với schema
- **Ưu tiên:** MEDIUM
- **Trạng thái:** ✅ done

## Task 4: DRY cross-cutting scope code trong automation

- **File:** `.claude/workflows/automation/workflow-sdlc-automation.js`
- **Dòng:** 638-715
- **Hiện tại:** 5 khối if-else gần như identical
- **Target:** Extract CC_AGENTS config array, loop qua array
- **Ưu tiên:** LOW
- **Trạng thái:** ✅ done

## Task 5: Pass parameters tường minh cho runGateWithRetry trong cook

- **File:** `.claude/workflows/cook/workflow-sdlc-cook.js`
- **Dòng:** 411 (function signature)
- **Hiện tại:** `async function runGateWithRetry(mode, totalChecks, phaseName, tcResultsFiltered)`
- **Target:** Thêm `techStackHint` và `allFiles` làm parameters
- **Ưu tiên:** LOW
- **Trạng thái:** ✅ done

## Task 6: Thêm idempotent resume cho automation

- **File:** `.claude/workflows/automation/workflow-sdlc-automation.js`
- **Pattern tham khảo:** `workflow-sdlc-cook.js` (resumeFrom)
- **Thay đổi:**
  - Thêm `resumeFrom` vào destructured args
  - `resumeFrom.completedPhases` — array các phase đã hoàn thành
  - `resumeFrom.phaseResults` — object chứa kết quả của từng phase đã chạy
  - Logic skip phase ở đầu mỗi phase section
- **Ưu tiên:** MEDIUM
- **Trạng thái:** ✅ done

## Task 7: Thêm idempotent resume cho codebase-reverse

- **File:** `.claude/workflows/codebase/workflow-codebase-reverse.js`
- **Pattern tham khảo:** `workflow-sdlc-cook.js` (resumeFrom)
- **Thay đổi:**
  - Thêm `resumeFrom` vào destructured args
  - `resumeFrom.completedPhases` — array các phase đã hoàn thành
  - `resumeFrom.phaseResults` — object chứa kết quả của từng phase đã chạy
  - Logic skip phase ở đầu mỗi phase section
- **Ưu tiên:** MEDIUM
- **Trạng thái:** ✅ done

---

## Thứ Tự Thực Hiện Đề Xuất

```
1. Task 1 (HIGH) → Sửa repoPath
2. Task 2 (HIGH) → Thêm schema
3. Task 3 (MEDIUM) → Parallelize verify
4. Task 6 (MEDIUM) → Idempotent resume automation
5. Task 7 (MEDIUM) → Idempotent resume codebase-reverse
6. Task 4 (LOW) → DRY CC code
7. Task 5 (LOW) → Fix runGateWithRetry params
```

---

*Generated by Claude Code*
